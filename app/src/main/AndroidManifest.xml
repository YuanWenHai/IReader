<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.will.Stardust">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGER"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:largeHeap="true">
        <activity
            android:name="com.will.Stardust.activity.LogoActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="com.will.Stardust.activity.MainPageActivity"
            android:launchMode="singleTask"></activity>
        <activity android:name="com.will.Stardust.file.FileSelector"
            android:launchMode="singleTask"></activity>
        <activity android:name="com.will.Stardust.activity.BookPage"
            android:launchMode="singleTask"></activity>
        <activity android:name="com.will.Stardust.activity.BookmarkPage"
            android:launchMode="singleTask"></activity>
        <activity android:name=".activity.DeleteBooks"
            android:launchMode="singleTask"></activity>
    </application>

</manifest>
